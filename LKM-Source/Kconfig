menu "Re:Kernel"

config REKERNEL
	bool "Re:Kernel module"
	depends on MODULES
	default n
	help
	  Make tombstone users get a better experience.
	  Tips: CLEAN_UP_ASYNC_BINDER and NETWORK_FILTER is enabled by default. Enabling this will build Re-Kernel as a module
endmenu
